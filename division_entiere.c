#include<stdio.h>
int main(){
	int n, m, q;
	int r=scanf;
	while(n!=(int)n & m!=(int)m || m!=0 ){
		printf("Entrez deux nombres n et m\n");
		scanf("%d %d", &n, &m);
		printf("Calcul du quotient\n");
		q=n/m;

		printf("La division de %d par %d est %d\n", n, m, q);
	}

	return 0;
}
